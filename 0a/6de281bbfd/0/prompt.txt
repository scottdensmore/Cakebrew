Is everything ready to create a pr and ready to merge? You create two background tasks that are now finished

---

<task-notification>
<task-id>bqwd7f2p7</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/00a7d348-fdab-4cac-8b78-0fd30c4f09b7/tasks/bqwd7f2p7.output</output-file>
<status>completed</status>
<summary>Background command "Poll until Copilot reviews PR #3" completed (exit code 0)</summary>
</task-notification>

---

Yes do both

---

I added entire to the project so we need to check in those changes

---

I want to rename the master branch to main and make it default. Then lets make sure everything is in sync with origina and local

---

So this was a fork but I can't leave the fork network because someone forked this version?

---

lets remember this and do this later

---

I want to make sure this app works and looks great on Tahoe. I want to make sure that the minum OS the last supported OS from Apple.

---

I want to make sure this app works and looks great on Tahoe. I want to make sure that the minum OS the last supported OS from Apple. What features can we add to make sure this looks good and adopts Tahoe look and feel.

---

Ok lets do those in order.

---

check the pr and see if there are any comments before you move on and address them. Once they are done you can merge and move to Tier 2

---

have all the local changes been merged into this branch?

---

Are there any background tasks that need to be cleaned up?

---

Yes. Lets pick up where you left off

---

1

---

<task-notification>
<task-id>bd0n3bpa5</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bd0n3bpa5.output</output-file>
<status>completed</status>
<summary>Background command "Poll until Copilot reviews PR #7" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>bhwgycxde</task-id>
<tool-use-id>toolu_01NB4meaToDRHuDEEV9ZWTY5</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bhwgycxde.output</output-file>
<status>completed</status>
<summary>Background command "Poll until Copilot reviews PR #9" completed (exit code 0)</summary>
</task-notification>

---

just leave it

---

For this project follow the following workflow
The 9-step pipeline
Branch — Cut a feature branch off the latest main (fix/…, feat/…, refactor/…, docs/…, chore/…). Never commit to main directly.
TDD (red → green → refactor) — Write the test first:
RED: smallest test that captures the next behaviour (or reproduces a bug); run it, confirm it fails for the right reason.
GREEN: minimum production code to make it pass.
REFACTOR: tidy code/test while keeping the suite green.
The test and the code that satisfies it land in the same commit, and the commit message names what the test covers.
Review the code with a UI subagent that is in an expert at building Apple apps for all OSs.
Verify with the verifier subagent — Run it over the pending change. it should build, run tests, linters 
Pre-PR review with the code-reviewer subagent — Run it over the diff; address every must-fix (and reasonable should-fix) finding. Required. If fixes change code, re-run the verifier.
Open a PR — Push and open via gh (GitHub CLI, never the web UI). Describe what changed, why, and how it was tested.
Green CI is the merge gate — Required checks should also be green. Never merge with a pending/failing required check. A pending Copilot/automated review is not part of the gate.
Address review feedback as it lands — Act on valid points, push fixes, reply/resolve threads, let CI re-run. Post-merge automated feedback is a follow-up, never a blocker.
Merge & clean up — Only this step merges: gh pr merge --merge --delete-branch, then return to main, git pull --ff-only, delete the local branch.
Key principles
One PR per logical unit of work — keep unrelated changes on separate branches/PRs.
Every behaviour change ships with a test, written first — bug fixes, features, refactors that change observable behaviour. Only pure formatting is exempt.
No file-header boilerplate in new .swift files (no Xcode template comment block).

---

actually I do want a squash merge

---

Look through the app, make sure we have plenty of unit test coverage and also covering all the critical user journeys. Ensure we have UI tests for the journeys.

---

Ok what is next?

---

<task-notification>
<task-id>bd0zl3q6x</task-id>
<tool-use-id>toolu_01QQ1xB1TU6LH4TncZh186Xe</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bd0zl3q6x.output</output-file>
<status>completed</status>
<summary>Background command "Watch CI on PR #12 to completion" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>b1xu5po8s</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/b1xu5po8s.output</output-file>
<status>completed</status>
<summary>Background command "Watch CI on PR #13 to completion" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>baamhbygj</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/baamhbygj.output</output-file>
<status>completed</status>
<summary>Background command "Watch CI (both jobs) on PR #14" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>b5bsj9dbi</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/b5bsj9dbi.output</output-file>
<status>completed</status>
<summary>Background command "Watch CI on PR #15 (incl. UI Tests job)" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>bncbqdvti</task-id>
<tool-use-id>toolu_01Af1qL88NdgKZJCSQomsUAQ</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bncbqdvti.output</output-file>
<status>completed</status>
<summary>Background command "Wait for retry loop to resolve, then report state" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>b9ekcjuoi</task-id>
<tool-use-id>toolu_01952SsWtsGnhSHLX91o8p5X</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/b9ekcjuoi.output</output-file>
<status>completed</status>
<summary>Background command "Watch CI on PR #16 (UI Tests runs the mock journey)" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>b6quzok5f</task-id>
<tool-use-id>toolu_01D2KiWqKT539WYp89B9jKpQ</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/b6quzok5f.output</output-file>
<status>completed</status>
<summary>Background command "Wait for diagnostic CI run to complete" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>b4prgwjva</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/b4prgwjva.output</output-file>
<status>completed</status>
<summary>Background command "Watch CI for the fix commit" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>bezg0x8k1</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bezg0x8k1.output</output-file>
<status>completed</status>
<summary>Background command "Open PR #17 and watch CI to completion" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>bd10qs6s2</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bd10qs6s2.output</output-file>
<status>completed</status>
<summary>Background command "Open PR #18 and watch CI" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>b5uj86106</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/b5uj86106.output</output-file>
<status>completed</status>
<summary>Background command "Watch CI on PR #19" completed (exit code 0)</summary>
</task-notification>

---

What is next?

---

Extend Cakebrew's XCUITest coverage (CakebrewUITests target) with two operation journeys that were deferred during the test-coverage work, building on the existing mock-brew infrastructure.

Context: the app has a fixture-backed mock brew interface (Cakebrew/BPMockHomebrewInterface.{h,m}) activated by launching with the `-BPMockBrew` argument (gated in +[BPHomebrewInterface sharedInterface]). CakebrewUITests/CakebrewUITests.m has a `launchWithArguments:` helper and a `formulaCellWithName:` helper (formula table cells are NSTextField elements matched by `value ==` predicate). UI tests are verified in CI (the "UI Tests" job in .github/workflows/ci.yml) because local XCUITest automation is unavailable; each iteration is a CI round-trip, so write defensively and add an `NSLog(self.app.debugDescription)` tree-dump on failure.

1. **Full install confirmation click-through.** Select a not-installed formula (e.g. mockhtop under "All Formulae"), click the toolbar "Install Formula" button, and handle the confirmation dialog. NOTE: installFormula: in BPHomebrewViewController.m uses `[alert runModal]` (app-modal NSAlert with "Yes"/"Cancel" buttons) — a click() that triggers runModal can hang XCUITest. Use `addUIInterruptionMonitorWithDescription:handler:` (or test the modal carefully) to dismiss it, and assert the install flow reached confirmation. If runModal proves too fragile, consider whether the production code should use a sheet (beginSheetModalForWindow:) instead.

2. **Update Homebrew run journey** (like the existing Doctor run test). The mock should override `updateWithReturnBlock:` to stream a recognizable string (e.g. "MOCK_UPDATE_OK"). The journey navigates to the Update tool view and clicks its "Update Homebrew" button — but NOTE the toolbar ALSO has an "Update Homebrew" button, so `app.buttons["Update Homebrew"]` is ambiguous; disambiguate (e.g. scope to the content area vs `app.toolbars`, or use elementBoundByIndex after inspecting a tree dump). Assert the streamed output appears in the Update view's text view (match `app.textViews` by `value CONTAINS`).

Follow the project's 9-step workflow (memory: feedback-git-workflow): branch off main, TDD, build + CI green gate, PR via gh, squash-merge. One PR per journey is fine.

---

<task-notification>
<task-id>b1sryyeh6</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/b1sryyeh6.output</output-file>
<status>completed</status>
<summary>Background command "Open PR #20 and watch CI" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>b5qgjhakh</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/b5qgjhakh.output</output-file>
<status>completed</status>
<summary>Background command "Watch CI on #21 with hang detection" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>b5vdvizv2</task-id>
<tool-use-id>toolu_01QztQow7GBhXoG4c1n52tSX</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/b5vdvizv2.output</output-file>
<status>completed</status>
<summary>Background command "Watch CI for the sheet-conversion commit" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>bbt7dfiw5</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bbt7dfiw5.output</output-file>
<status>completed</status>
<summary>Background command "Watch CI for the window-fix commit" completed (exit code 0)</summary>
</task-notification>

---

What is the priority list to do next?

---

Yep lets kickoff #1

---

<task-notification>
<task-id>bmwafr63h</task-id>
<tool-use-id>toolu_01AQhrXvqJAoPckvjxzGqgBc</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bmwafr63h.output</output-file>
<status>completed</status>
<summary>Background command "Open PR #22 and watch CI" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>bf2x3pd5w</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bf2x3pd5w.output</output-file>
<status>completed</status>
<summary>Background command "Compile, commit Escape fix, push, watch CI" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>b7jghesbl</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/b7jghesbl.output</output-file>
<status>completed</status>
<summary>Background command "Commit CI crash-capture, push, watch run" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>b5iy3yfnw</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/b5iy3yfnw.output</output-file>
<status>completed</status>
<summary>Background command "Build, commit mock-info fix, push, watch CI" completed (exit code 0)</summary>
</task-notification>

---

BPFormula getInformation (Cakebrew/Models/BPFormula.m) can crash the app when `brew info <name>` returns output that doesn't match the expected format. Specifically, around line 267-268:

    line = [lines objectAtIndex:lineIndex];
    [self setLatestVersion:[line substringFromIndex:[line rangeOfString:@":"].location+2]];

If the first line has no ":" then `rangeOfString:@":"].location` is NSNotFound (a huge value), `+2` overflows, and `substringFromIndex:` throws an NSRangeException → SIGABRT (uncaught on a background queue). This was hit in CI when a formula's info was loaded for a name brew didn't recognize; it's a real crash risk in production whenever brew returns unexpected output (errors, new formats, taps, etc.).

Harden the parser so it never crashes on malformed input:
- Guard the line-0 ":" lookup: if no ":" (NSNotFound) or fewer lines than expected, bail gracefully (treat like the existing empty/`Error`-prefix early-exit paths that set `_needsInformation = NO` and return).
- Audit the other unguarded `objectAtIndex:`/`substringFromIndex:` calls in getInformation (lineIndex increments, the conflicts/installPath section) for the same out-of-range risk and guard them too.
- Add unit tests in CakebrewTests/BPFormulaTests.m feeding malformed inputs (no colon on line 0, truncated output, empty-ish multi-line) and asserting getInformation returns without crashing. There are existing brewInfo_*.txt fixtures and a BPCustomFormula test pattern to follow.

Follow the project's 9-step workflow (memory: feedback-git-workflow): branch off main, test-first, build + green CI gate, PR via gh, squash-merge.

---

<task-notification>
<task-id>b2dak7n92</task-id>
<tool-use-id>toolu_01GbXNfqaA9KXeT1ubQhuPxj</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/b2dak7n92.output</output-file>
<status>completed</status>
<summary>Background command "Open PR #24 and watch CI" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>b8dw3lsx6</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/b8dw3lsx6.output</output-file>
<status>completed</status>
<summary>Background command "Open PR #25, watch CI" completed (exit code 0)</summary>
</task-notification>

---

Ok what is the list of next things?

---

Ok lets start with the search journey

---

<task-notification>
<task-id>b4mhse32f</task-id>
<tool-use-id>toolu_015FDXpE3o8XRsTzBTPS1wTa</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/b4mhse32f.output</output-file>
<status>completed</status>
<summary>Background command "Compile, commit, push, open PR #26, watch CI" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>bdexijm67</task-id>
<tool-use-id>toolu_01Qfy4TjZ6rxQyALLTmYwXNL</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bdexijm67.output</output-file>
<status>completed</status>
<summary>Background command "Compile, commit Cmd+F fix, push, watch CI" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>bpsp4zagd</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bpsp4zagd.output</output-file>
<status>completed</status>
<summary>Background command "Commit diagnostic, push, wait for CI" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>bednyict7</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bednyict7.output</output-file>
<status>completed</status>
<summary>Background command "Commit coordinate-click fix, push, watch CI, extract field value" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>bzh8rvrt5</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bzh8rvrt5.output</output-file>
<status>completed</status>
<summary>Background command "Commit pivot, push, watch CI" completed (exit code 0)</summary>
</task-notification>

---

keep going with the backlog

---

<task-notification>
<task-id>bi0d2fzza</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bi0d2fzza.output</output-file>
<status>completed</status>
<summary>Background command "Compile, commit, push, open PR #27, watch CI" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>bj3shwkyf</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bj3shwkyf.output</output-file>
<status>completed</status>
<summary>Background command "Build, commit, push, open PR #28, watch CI" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>b763rrzx0</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/b763rrzx0.output</output-file>
<status>completed</status>
<summary>Background command "Commit, push, open PR #29, watch CI" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>bdmj942gf</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bdmj942gf.output</output-file>
<status>completed</status>
<summary>Background command "Commit, push, open PR #30, watch CI" completed (exit code 0)</summary>
</task-notification>

---

Lets add the thin import/export check

---

<task-notification>
<task-id>bdk3jmbv2</task-id>
<tool-use-id>toolu_01U35zu36AXznUtpMn2asiJe</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/bdk3jmbv2.output</output-file>
<status>completed</status>
<summary>Background command "Compile, commit, push, open PR #31, watch CI" completed (exit code 0)</summary>
</task-notification>

---

What is next?

---

Ok lets get going on those

---

I want to make sure the app supports the latest macOS SDK and minium latest minus two

---

Actually lets only do SDK minus 1. Since that is usally what Apple supports

---

I am getting this warning /Users/scottdensmore/Developer/scottdensmore/cakebrew/CakebrewTests/BPFormulaeDataSourceTests.m:44:53 Null passed to a callee that requires a non-null argument

---

what is the workflow you have for this project

---

What is the next thing we should work on for this app?

---

Yes lets do that

---

Yes

---

yes

---

Lets plan out some new features that would be great for this app.

---

<task-notification>
<task-id>afaac19e1267102f2</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/afaac19e1267102f2.output</output-file>
<status>completed</status>
<summary>Agent "Map data &amp; command layer" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Perfect. Now I have all the information I need. Let me create a comprehensive summary document:

## Cakebrew Data/Command Layer: Extension Points Analysis

### 1. CORE COMMAND EXECUTION

**Central method:** `BPHomebrewInterface.m:248-290`  
**Method signature:**
```objc
- (BOOL)performAsyncBrewCommandWithArguments:(NSArray*)arguments
                     wrapsSynchronousRequest:(BOOL)isSynchronous
                                       queue:(dispatch_queue_t)queue
                             dataReturnBlock:(void (^)(NSString*))block
```

**How it works:**
- **Argument assembly:** `formatArguments:sendOutputId:` at line 211 wraps the `arguments` array into a shell command string:
  ```objc
  // Async (live UI updates): echo "marker"; brew &lt;args&gt;
  // Sync (block until done): brew &lt;args&gt;
  command = [NSString stringWithFormat:@"brew %@", [extraArguments componentsJoinedByString:@" "]];
  ```
  Arguments are space-joined directly — no escaping, just concatenation.

- **Brew executable path:** Resolved via user shell (e.g., `/bin/bash -l`) at line 119. The shell inherits the user's `$PATH`, which includes the Homebrew binary location. The app does NOT hardcode a brew path; it relies on the shell's login environment to find `brew`.

- **Shell path:** Retrieved in `getValidUserShellPath` (line 152) from `$SHELL` environment variable, validated against `/etc/shells`.

- **Execution:** Line 260 creates a `BPTask` with:
  ```objc
  BPTask *task = [[BPTask alloc] initWithPath:self.path_shell 
                                     arguments:@[@"-l", @"-c", command]];
  ```
  The shell runs with `-l` (login shell, loads profile) and `-c` (execute the formatted command).

- **Output streaming:** If a `dataReturnBlock` callback is provided, `BPTask` streams partial output in real-time via `NSFileHandle` notifications (BPTask.m:107-114, 122-129). Otherwise, waits for task completion.

**Synchronous wrapper:** `performSyncBrewCommandWithArguments:` (line 305) uses `dispatch_sync` on a dedicated queue to make async calls block until the output is fully accumulated.

**Funnel pattern:** All high-level operations (`installFormula:withOptions:andReturnBlock:`, `uninstallFormula:withReturnBlock:`, `upgradeFormulae:withReturnBlock:`, `tapRepository:withReturnsBlock:`, `update`, `cleanup`, etc.) call either:
- `performBrewCommandWithArguments:dataReturnBlock:` (async, for UI streaming)
- `performSyncBrewCommandWithArguments:` (sync, blocks until done)

**To add a new subcommand (e.g., `brew services list`, `brew pin &lt;formula&gt;`):**
1. Create a new method in `BPHomebrewInterface` that assembles the args as an `NSArray` (e.g., `@[@"services", @"list"]`).
2. Call `performBrewCommandWithArguments:dataReturnBlock:` (async with UI callback) or `performSyncBrewCommandWithArguments:` (sync).
3. The args array is automatically formatted and passed to the shell. No additional escaping needed.

---

### 2. DATA MODEL &amp; MANAGER

**Manager class:** `BPHomebrewManager.h:41-64`, `BPHomebrewManager.m:37-105`

**Public properties (list containers):**
```objc
@property (strong) NSArray&lt;BPFormula*&gt; *installedFormulae;      // from kBPListInstalled
@property (strong) NSArray&lt;BPFormula*&gt; *outdatedFormulae;       // from kBPListOutdated
@property (strong) NSArray&lt;BPFormula*&gt; *allFormulae;            // from kBPListAll (cached, 24h)
@property (strong) NSArray&lt;BPFormula*&gt; *leavesFormulae;         // from kBPListLeaves
@property (strong) NSArray&lt;BPFormula*&gt; *searchFormulae;         // populated by updateSearchWithName:
@property (strong) NSArray&lt;BPFormula*&gt; *repositoriesFormulae;   // from kBPListRepositories
```

**Refresh method:** `reloadFromInterfaceRebuildingCache:` (line 73)
- Dispatches to background queue
- Calls `listMode:` for installed, leaves, outdated, repositories (always fresh)
- Calls `listMode:kBPListAll` only if cache is missing or older than 24h (line 86)
- Updates all properties on main queue (line 90-100)
- Invokes `homebrewManagerFinishedUpdating:` delegate callback (line 102)

**Status determination:** `statusForFormula:` (line 237) searches the installed/outdated lists to return `kBPFormulaInstalled`, `kBPFormulaOutdated`, or `kBPFormulaNotInstalled`.

**BPListMode enum** (BPHomebrewInterface.h:26-33):
```objc
typedef NS_ENUM(NSInteger, BPListMode) {
    kBPListAll,            // all available
    kBPListInstalled,      // installed
    kBPListLeaves,         // installed but unused as dependency
    kBPListOutdated,       // installed but newer version available
    kBPListSearch,         // NOT for direct listMode: call; client-side filtering
    kBPListRepositories    // tapped repos
};
```

**To add a new list (e.g., installed casks, services):**
1. Add a new `kBPListCasks` case to `BPListMode` enum (BPHomebrewInterface.h:26-33)
2. Add a `@property (strong) NSArray&lt;BPFormula*&gt; *casksFormulae;` to `BPHomebrewManager.h:43-48`
3. In `BPHomebrewManager.m:reloadFromInterfaceRebuildingCache:`, add:
   ```objc
   NSArray *casksFormulae = [[BPHomebrewInterface sharedInterface] listMode:kBPListCasks];
   [self setCasksFormulae:casksFormulae];
   ```
4. In `BPHomebrewInterface.m:listMode:` (line 330), add a switch case creating a new `BPHomebrewInterfaceListCall` subclass (see section 3).
5. Implement the mock in `BPMockHomebrewInterface.m:listMode:` (line 20) to return fixture data.

---

### 3. LISTMODE → BREW CALL → PARSING

**Entry point:** `BPHomebrewInterface.m:330-369`
```objc
- (NSArray&lt;BPFormula *&gt; *)listMode:(BPListMode)mode
```

**Pattern:** Each mode instantiates a subclass of `BPHomebrewInterfaceListCall` (base at line 517):
- `BPHomebrewInterfaceListCallInstalled` (line 551): args `@[@"list", @"--versions"]`
- `BPHomebrewInterfaceListCallAll` (line 566): args `@[@"formulae"]`
- `BPHomebrewInterfaceListCallLeaves` (line 575): args `@[@"leaves"]`
- `BPHomebrewInterfaceListCallUpgradeable` (line 584): args `@[@"outdated", @"--verbose"]` (parses three fields: name, installed version, latest version)
- `BPHomebrewInterfaceListCallRepositories` (line 623): args `@[@"tap"]` (lists tapped repos)

**Parsing flow (line 359-368):**
```objc
NSString *string = [self performSyncBrewCommandWithArguments:listCall.arguments];
if (string) {
    return [listCall parseData:string];  // splits by \n, parses each line
}
```

**Base parseData** (line 528): Splits by newline, drops the trailing empty line, calls `parseFormulaItem:` for each line.

**Base parseFormulaItem** (line 544): Returns `[BPFormula formulaWithName:item]` — name only, no version.

**Installed subclass parseFormulaItem** (line 558):
```objc
NSArray *aux = [item componentsSeparatedByString:@" "];
return [BPFormula formulaWithName:[aux firstObject] andVersion:[aux lastObject]];
```
Splits by space, takes first token as name, last as version. Handles multiple versions by taking the last one.

**Outdated subclass parseFormulaItem** (line 591): Uses regex `"(\\S+)\\s\\(((.*, )*(.*))\\) &lt; (\\S+)"` to extract:
- Capture group 1: formula name
- Capture group 3: installed version (last of comma-separated list)
- Capture group 5: latest version
Creates `BPFormula` with all three fields; falls back to name-only if regex fails.

**To add a new list subclass (e.g., for casks or services):**
1. Declare in BPHomebrewInterface.m:
   ```objc
   @interface BPHomebrewInterfaceListCallCasks : BPHomebrewInterfaceListCall
   @end
   ```
2. Implement:
   ```objc
   @implementation BPHomebrewInterfaceListCallCasks
   - (instancetype)init {
       return (BPHomebrewInterfaceListCallCasks *)[super initWithArguments:@[@"list", @"--cask"]];
   }
   - (BPFormula *)parseFormulaItem:(NSString *)item {
       // Parse line format (adjust as needed)
       NSArray *aux = [item componentsSeparatedByString:@" "];
       return [BPFormula formulaWithName:[aux firstObject] andVersion:[aux lastObject]];
   }
   @end
   ```
3. Add switch case in `listMode:` to instantiate it:
   ```objc
   case kBPListCasks:
       listCall = [[BPHomebrewInterfaceListCallCasks alloc] init];
       break;
   ```

---

### 4. FORMULA INFO &amp; DEPENDENTS

**Dependencies field:** `BPFormula` properties (BPFormula.h:40-41):
```objc
@property (nonatomic, copy, readonly) NSString *dependencies;  // semicolon-separated
@property (nonatomic, copy, readonly) NSString *conflicts;     // comma-separated
```

**Fetching &amp; parsing:** Triggered by `needsInformation` property change (BPFormula.m:180-203):
- Sets `needsInformation = YES` → KVO fires → calls `getInformation` (line 215)
- Calls `informationForFormulaName:` on the data provider (BPHomebrewInterface)
- Parses the `brew info &lt;name&gt;` output

**`informationForFormulaName:`** (BPHomebrewInterface.m:371-374):
```objc
- (NSString *)informationForFormulaName:(NSString *)name;
{
    return [self performSyncBrewCommandWithArguments:@[@"info", name]];
}
```
Returns the raw `brew info` output as a string. No parsing at the interface level.

**Parsing in BPFormula.getInformation** (BPFormula.m:215-419):
- Splits output by newlines (line 264)
- Line 1: extracts latest version
- Lines 2-4: homepage URL and short description (detects if line looks like a URL via `BPLineLooksLikeWebsite`)
- Looks for "Conflicts with:" section (line 299-326) and extracts conflicts
- Extracts install path (line 328-336)
- Finds dependencies by text range between "==&gt; Dependencies" and the next section (line 338-377)
- Finds options by text range between "==&gt; Options" and "==&gt; Caveats" (line 380-408)
- Stores all in writable properties (dependencies, conflicts, shortDescription, installPath, options)

**Dependents (reverse dependencies):** `dependantsForFormulaName:onlyInstalled:` (BPHomebrewInterface.m:376-388):
```objc
- (NSString *)dependantsForFormulaName:(NSString *)name onlyInstalled:(BOOL)onlyInstalled
{
    NSMutableArray *arguments = [NSMutableArray arrayWithObject:@"uses"];
    if (onlyInstalled) {
        [arguments addObject:@"--installed"];
    }
    [arguments addObject:name];
    return [self performSyncBrewCommandWithArguments:arguments];
}
```
- Runs `brew uses [--installed] &lt;name&gt;`
- Returns raw string output (not parsed at interface level)
- Caller must parse the returned string

**Note:** Currently NO dependents parsing is implemented in the UI layer. The interface returns the raw output; the caller must parse lines manually.

**To add a dependents viewer:**
1. Create a new method in `BPHomebrewInterface` that calls `dependantsForFormulaName:onlyInstalled:` (already exists) to get the list.
2. In `BPFormula` (or a new ViewModel), parse the output lines into formula names and create a display-friendly format.
3. Add a `@property (copy) NSString *dependents;` to `BPFormula` (analogous to dependencies).
4. Expose via `BPFormulaPopoverViewController` (or similar).

**To add support for dependencies with versions:**
1. The current `dependencies` property is a raw string split by `"; "` (BPFormula.m:374).
2. Parse it into structured objects (name + constraint) if needed, or keep as display string.

---

### 5. CASK, SERVICES, PIN/UNPIN SUPPORT TODAY

**Finding:** **NONE exist** in the data/command layer.

**Mock interface references:** `BPMockHomebrewInterface.m:42` includes a mock tap named `@"homebrew/cask"` in the repositories fixture, but no actual cask list, install, or management methods are implemented.

**No methods for:**
- `brew install --cask` (cask installation)
- `brew list --cask` (list installed casks)
- `brew services list/start/stop` (service management)
- `brew pin/unpin` (version pinning)

**To add cask support:**
1. Add `kBPListInstalledCasks` and `kBPListAllCasks` to `BPListMode` enum
2. Create `BPHomebrewInterfaceListCallInstalledCasks` subclass with args `@[@"list", @"--cask"]`
3. Add parsing for cask list output (typically same format as formulae)
4. Add `installedCasksFormulae` property to `BPHomebrewManager`
5. Add install/uninstall methods like:
   ```objc
   - (BOOL)installCask:(NSString*)cask withReturnBlock:(void (^)(NSString*))block
   {
       return [self performBrewCommandWithArguments:@[@"install", @"--cask", cask] 
                                       dataReturnBlock:block];
   }
   ```
6. Update `BPMockHomebrewInterface.listMode:` to return fixture casks

**To add services support:**
1. Add `kBPListServices` to enum (or `kBPListServicesRunning`, `kBPListServicesAll`)
2. Create `BPHomebrewInterfaceListCallServices` with args `@[@"services", @"list", @"--json=v2"]`
3. Parse JSON output (Homebrew 4.0+ returns JSON) into formula objects with a "status" field
4. Add methods:
   ```objc
   - (BOOL)startService:(NSString*)service withReturnBlock:(void (^)(NSString*))block
   {
       return [self performBrewCommandWithArguments:@[@"services", @"start", service] 
                                       dataReturnBlock:block];
   }
   // ... stop, restart, etc.
   ```

**To add pin/unpin:**
1. Add methods directly to `BPHomebrewInterface`:
   ```objc
   - (BOOL)pinFormula:(NSString*)formula withReturnBlock:(void (^)(NSString*))block
   {
       return [self performBrewCommandWithArguments:@[@"pin", formula] 
                                       dataReturnBlock:block];
   }
   - (BOOL)unpinFormula:(NSString*)formula withReturnBlock:(void (^)(NSString*))block
   {
       return [self performBrewCommandWithArguments:@[@"unpin", formula] 
                                       dataReturnBlock:block];
   }
   ```
2. Add a `pinned` property to `BPFormula` (or fetch from `brew pin --list-pinned`)
3. Update UI to show pin status and call these methods

---

### 6. TEST SEAMS &amp; MOCK INTERFACE

**Mock activation:** `BPHomebrewInterface.m:70-91`
```objc
+ (instancetype)sharedInterface
{
    @synchronized(self) {
        static dispatch_once_t once;
        static BPHomebrewInterface *instance;
        dispatch_once(&amp;once, ^ {
            Class interfaceClass = [BPHomebrewInterface class];
            if ([[[NSProcessInfo processInfo] arguments] containsObject:@"-BPMockBrew"]) {
                Class mockClass = NSClassFromString(@"BPMockHomebrewInterface");
                if (mockClass) {
                    interfaceClass = mockClass;
                }
            }
            instance = [[interfaceClass alloc] initUniqueInstance];
        });
        return instance;
    }
}
```

When the app is launched with `-BPMockBrew` argument, the singleton returns a `BPMockHomebrewInterface` instance instead of `BPHomebrewInterface`.

**Mock interface methods** (BPMockHomebrewInterface.m:1-93):
- `checkForHomebrew` (line 12): Always returns `YES`
- `listMode:` (line 20): Returns hardcoded fixture lists (mockwget, mockgit, etc.)
- `runDoctorWithReturnBlock:` (line 50): Returns fixed string
- `updateWithReturnBlock:` (line 59): Returns fixed string
- `runCleanupWithReturnBlock:` (line 68): Returns fixed string
- `informationForFormulaName:` (line 79): Returns minimal info template
- `dependantsForFormulaName:onlyInstalled:` (line 88): Returns empty string

**Methods NOT overridden** (use real implementation):
- `installFormula:withOptions:andReturnBlock:`
- `uninstallFormula:withReturnBlock:`
- `upgradeFormulae:withReturnBlock:`
- `tapRepository:withReturnsBlock:`
- `untapRepository:withReturnsBlock:`
- `runBrewExportToolWithPath:`
- `runBrewImportToolWithPath:withReturnsBlock:`

These fall back to the real implementation, which will fail without a real Homebrew install.

**Test targets &amp; test files:**

| Test File | Purpose |
|-----------|---------|
| `CakebrewTests/BPHomebrewInterfaceListCallTests.m` | Tests the list-call parsers (installed, outdated, all, leaves, repositories) |
| `CakebrewTests/BPFormulaTests.m` | Tests `getInformation` parsing of `brew info` output with real fixtures (brewInfo_*.txt files) |
| `CakebrewTests/BPHomebrewManagerTests.m` | Tests manager's reload flow, status determination, search |
| `CakebrewTests/BPFormulaeDataSourceTests.m` | Tests UI data source filtering and sorting |
| `CakebrewTests/BPFormulaOptionTests.m` | Tests installation option parsing |

**To add test coverage for a new feature (e.g., services list):**
1. Create `BPHomebrewInterfaceServicesListCallTests.m` with a `BPHomebrewInterfaceListCallServices` forward declaration
2. Add test methods for parsing JSON output
3. Add fixture override in `BPMockHomebrewInterface.listMode:` for the new `kBPListServices` case
4. Add manager test in `BPHomebrewManagerTests.m` for the new property reload
5. Create a `brewInfo_serviceName.txt` fixture file if testing service info parsing

**Methods a new feature MUST mock:**
- If it returns a list → override `listMode:` in the mock
- If it calls `informationForFormulaName:` → the mock already returns a template
- If it needs custom output → add a new mock method or override an existing one

---

### SUMMARY: EXTENSION CHECKLIST

| Feature | Required Changes |
|---------|------------------|
| **New `brew` subcommand** | Create interface method → call `performBrewCommandWithArguments:` or `performSyncBrewCommandWithArguments:` |
| **New list (e.g., casks)** | Add `kBPListCasks` to enum → add `BPHomebrewInterfaceListCallCasks` subclass → add manager property → add mock fixture |
| **New formula property** | Add `@property` to `BPFormula` → parse in `getInformation` or via a separate fetch |
| **Dependents viewer** | Already have `dependantsForFormulaName:onlyInstalled:` → parse output lines in UI layer |
| **Background task (outdated updater)** | Use `performBrewCommandWithArguments:dataReturnBlock:` with a timer or daemon; trigger `reloadFromInterfaceRebuildingCache:` when done |
| **Pin/unpin** | Add interface methods `pinFormula:withReturnBlock:` and `unpinFormula:withReturnBlock:` → add UI |
| **Services** | Add `kBPListServices` → parse `brew services list --json=v2` → add start/stop/restart methods → add manager property |

---

## File:Line Reference Summary

| Question | File:Line |
|----------|-----------|
| Core command executor | BPHomebrewInterface.m:248-290 |
| Argument formatting | BPHomebrewInterface.m:211-222 |
| Shell/path resolution | BPHomebrewInterface.m:152-189 (shell), 119 (which brew) |
| BPTask (subprocess) | BPTask.h:33-46, BPTask.m:57-83 |
| listMode entry | BPHomebrewInterface.m:330-369 |
| BPListMode enum | BPHomebrewInterface.h:26-33 |
| Manager reload | BPHomebrewManager.m:73-105 |
| Manager properties | BPHomebrewManager.h:43-48 |
| Manager status | BPHomebrewManager.m:237-254 |
| informationForFormulaName | BPHomebrewInterface.m:371-374 |
| dependantsForFormulaName | BPHomebrewInterface.m:376-388 |
| BPFormula getInformation | BPFormula.m:215-419 |
| Dependencies parsing | BPFormula.m:338-377 |
| Conflicts parsing | BPFormula.m:299-326 |
| Mock interface | BPMockHomebrewInterface.m:1-93 |
| Mock activation | BPHomebrewInterface.m:70-91 |
| List call parsers (installed) | BPHomebrewInterface.m:551-564 |
| List call parsers (outdated) | BPHomebrewInterface.m:584-620 |
| Test files location | CakebrewTests/*.m |</result>
<usage><subagent_tokens>50167</subagent_tokens><tool_uses>24</tool_uses><duration_ms>103387</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a643401d1b91ec7d1</task-id>
<tool-use-id>toolu_01AmiwZUZt9UrmXounjUWDqm</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-scottdensmore-Developer-scottdensmore-cakebrew/86f7230f-16a2-4201-9dae-105c1789c2c6/tasks/a643401d1b91ec7d1.output</output-file>
<status>completed</status>
<summary>Agent "Map UI &amp; app-shell layer" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Perfect. Now I have all the information I need. Let me compile a comprehensive, detailed summary with exact file:line references.

## CAKEBREW EXTENSION POINTS — DETAILED MAPPING

Based on my thorough exploration of the Cakebrew codebase, here are the exact extension points for your planned features:

---

### 1. SIDEBAR → CONTENT WIRING

**File:** `/Users/scottdensmore/Developer/scottdensmore/cakebrew/Cakebrew/Controllers/BPSideBarController.h` &amp; `.m`

**Enum Definition (lines 11–22, .h):**
```
typedef NS_ENUM(NSUInteger, FormulaeSideBarItem)
{
    FormulaeSideBarItemFormulaeCategory = 0,
    FormulaeSideBarItemInstalled = 1,
    FormulaeSideBarItemOutdated = 2,
    FormulaeSideBarItemAll = 3,
    FormulaeSideBarItemLeaves = 4,
    FormulaeSideBarItemRepositories = 5,
    FormulaeSideBarItemToolsCategory = 6,
    FormulaeSideBarItemDoctor = 7,
    FormulaeSideBarItemUpdate = 8,
};
```

**Sidebar Item Construction (BPSideBarController.m:161–210):**
- `buildSidebarTree()` creates a hierarchy using `BPSidebarItem` objects
- Formulae group items created at **lines 166–193** (Installed, Outdated, All, Leaves, Repositories)
- Tools group items created at **lines 195–209** (Doctor, Update)
- Each item has a title, identifier, icon, and optional badge value

**TO ADD A NEW SIDEBAR SECTION (e.g., "Casks"):**

1. **Update enum** in `BPSideBarController.h:11–22`: Add `FormulaeSideBarItemCasks = 9,` (or extend the Tools group with a new item)
2. **Add property** in `BPSideBarController.m:139–148` (private interface): `@property (strong, nonatomic) BPSidebarItem *casksFormulaeSidebarItem;`
3. **Build item in `buildSidebarTree()`** at `BPSideBarController.m:180–193`:
   ```
   _casksFormulaeSidebarItem = [BPSidebarItem itemWithTitle:NSLocalizedString(@"Sidebar_Item_Casks", nil) identifier:@"item"];
   _casksFormulaeSidebarItem.icon = [self casksSidebarIconImage];
   [parent addChildItem:_casksFormulaeSidebarItem];
   ```
4. **Add icon method** (following pattern at lines 212–252): e.g., `casksSidebarIconImage`
5. **Update badge refresh** in `refreshSidebarBadges()` (line 264–271): Add badge binding for casks
6. **Wire selection** in `BPHomebrewViewController.m:554–608` (`sourceListSelectionDidChange` delegate method): Add case for new sidebar index that calls `[self configureTableForListing:kBPListCasks]`

**Delegation Flow:**
- Sidebar delegate: `BPSideBarControllerDelegate` protocol defined at **BPSideBarController.h:24–26**
- Delegate callback: `sourceListSelectionDidChange()` → **BPHomebrewViewController.m:554–608**
- Maps sidebar row to `BPListMode` enum via switch statement
- Calls `configureTableForListing:` at **BPHomebrewViewController.m:344–351**

---

### 2. MAIN CONTENT TABLE — COLUMN CONFIGURATION &amp; MODE-SPECIFIC DISPLAY

**Files:**
- `BPFormulaeTableView.h` &amp; `.m`
- `BPFormulaeDataSource.h` &amp; `.m`

**Column Identifiers (BPFormulaeTableView.m:11–14):**
```
NSString * const kColumnIdentifierName = @"Name";
NSString * const kColumnIdentifierVersion = @"Version";
NSString * const kColumnIdentifierLatestVersion = @"LatestVersion";
NSString * const kColumnIdentifierStatus = @"Status";
```

**Mode-Based Column Logic (BPFormulaeTableView.m:37–108):**
`configureTableForListing()` method switches on `self.mode` (a `BPListMode` property):
- **kBPListAll** (lines 49–56): Shows Status column, hides Version &amp; LatestVersion
- **kBPListInstalled** (lines 58–65): Shows Version, hides LatestVersion &amp; Status
- **kBPListOutdated** (lines 75–83): Shows both Version &amp; LatestVersion columns
- **kBPListRepositories** (lines 94–100): Shows only Name, hides all others
- **kBPListLeaves** (lines 67–73): Shows only Name column

**Data Source Logic (BPFormulaeDataSource.m:95–143):**
`tableView:objectValueForTableColumn:row:` maps column identifier to data:
- **kColumnIdentifierName**: Returns `[(BPFormula*)element name]`
- **kColumnIdentifierVersion**: Returns `[(BPFormula*)element version]`
- **kColumnIdentifierLatestVersion**: Returns `[(BPFormula*)element shortLatestVersion]`
- **kColumnIdentifierStatus**: Returns status string via `[[BPHomebrewManager sharedManager] statusForFormula:element]`

**TO ADD CUSTOM COLUMNS (e.g., service status for "Services"):**

1. **Define new column identifier** in `BPFormulaeTableView.h:12–15` or add locally:
   ```
   NSString * const kColumnIdentifierServiceStatus = @"ServiceStatus";
   ```
2. **Add table column in XIB** (`MainMenu.xib:779–850`): Create `&lt;tableColumn identifier="ServiceStatus"&gt;` with header and cell
3. **Update mode logic** in `BPFormulaeTableView.m:37–108`, add case for new mode:
   ```
   case kBPListServices:
       [[self tableColumnWithIdentifier:kColumnIdentifierServiceStatus] setHidden:NO];
       [[self tableColumnWithIdentifier:kColumnIdentifierVersion] setHidden:YES];
       // configure widths...
   ```
4. **Implement data binding** in `BPFormulaeDataSource.m:95–143`:
   ```
   else if ([columnIdentifer isEqualToString:kColumnIdentifierServiceStatus]) {
       if ([element isKindOfClass:[BPFormula class]]) {
           return [(BPFormula*)element serviceStatus];
       }
   }
   ```

**KVO Observation (BPFormulaeTableView.m:20–26):**
- Table observes `mode` property changes via KVO
- Change triggers `configureTableForListing()` automatically

---

### 3. DETAIL / INFO PANE — STRUCTURE &amp; EXTENSION

**File:** `/Users/scottdensmore/Developer/scottdensmore/cakebrew/Cakebrew/Controllers/BPSelectedFormulaViewController.h` &amp; `.m`

**View Controller Definition (BPSelectedFormulaViewController.h:19–31):**
```
@interface BPSelectedFormulaViewController : NSViewController
@property (strong, nonatomic) NSArray *formulae;
@property (weak) id&lt;BPSelectedFormulaViewControllerDelegate&gt; delegate;
@property (weak) IBOutlet NSTextField *formulaDescriptionLabel;
@property (weak) IBOutlet NSTextField *formulaPathLabel;
@property (weak) IBOutlet NSTextField *formulaVersionLabel;
@property (weak) IBOutlet NSTextField *formulaDependenciesLabel;
@property (weak) IBOutlet NSTextField *formulaConflictsLabel;
@end
```

**XIB File:** `/Users/scottdensmore/Developer/scottdensmore/cakebrew/Cakebrew/Base.lproj/BPSelectedFormula.xib`
- Root custom view: `GxG-TU-x9D` (lines 21–200+)
- Stack view container: `udO-g0-WC6` (vertical layout, lines 24–194)
- **5 Label rows** (lines 35–179) in horizontal stack views:
  1. Description (lines 35–63)
  2. Location/Path (lines 64–92)
  3. Version (lines 93–121)
  4. Dependencies (lines 122–150)
  5. Conflicts (lines 151–179)

**Integration in Main View (BPHomebrewViewController.m:75–76, 180–194):**
- `selectedFormulaView` outlet points to split view's lower panel
- `BPSelectedFormulaViewController` embedded as child VC with auto-layout constraints

**Data Update Flow (BPSelectedFormulaViewController.m:53–168):**
- `setFormulae:` method (line 53) receives selected formula array
- KVO observer watches `BPFormulaDidUpdateNotification` (line 61–66)
- `displayInformationForFormulae()` method (line 84) populates all labels
- Single formula displays full info; multiple show "Multiple values"

**TO ADD DEPENDENTS / DEPENDENCIES VIEWER:**

1. **Add new outlet** in `BPSelectedFormulaViewController.h`:
   ```
   @property (weak) IBOutlet NSTableView *dependentsTableView;
   ```
2. **Create new table column** in `BPSelectedFormula.xib` (add `NSTableView` + data source/delegate)
3. **Add to XIB stack** in the main view container (`udO-g0-WC6`) — append new row above current layout
4. **Implement data source** in controller:
   ```
   - (NSInteger)numberOfRowsInTableView:(NSTableView *)tableView {
       return [[selectedFormula dependents] count];
   }
   ```
5. **Populate on formula change** in `displayInformationForFormulae()`:
   ```
   BPFormula *formula = [self.formulae firstObject];
   if (formula.dependents) {
       [self.dependentsTableView reloadData];
   }
   ```
6. **Alternative: Add as separate sub-view** using similar constraint-based embedding (like current info view)

**Split View Container (BPHomebrewViewController.m:282–286):**
- `formulaeSplitView` (outlet at line 75)
- Top: formulae table; Bottom: selected formula info
- Divider position managed at lines 282–285: `setPosition:ofDividerAtIndex:0`

---

### 4. PREFERENCES / SETTINGS INFRASTRUCTURE

**Current State:**
- **No preferences window exists.** Preferences menu item in MainMenu.xib:30 is **hidden and disabled**.
- Minimal `NSUserDefaults` usage for cache metadata only (not user-facing settings).

**NSUserDefaults Usage (BPHomebrewManager.m):**
- **Lines with standardUserDefaults:**
  - `kBPCacheLastUpdateKey` for cache timestamp (lines checking/setting via `NSUserDefaults`)
  - `BPBrewCellarPath` in `BPHomebrewInterface.m` (custom Homebrew path storage)

**TO BUILD PREFERENCES INFRASTRUCTURE:**

1. **Create preferences controller:**
   - New file: `/Cakebrew/Controllers/BPPreferencesWindowController.h` &amp; `.m`
   - Subclass `NSWindowController` with `NSTabViewController` for panes

2. **Enable menu item** in `MainMenu.xib:30`:
   - Remove `hidden="YES"` and `enabled="NO"` attributes
   - Wire to `IBAction` in `BPAppDelegate.m`:
     ```
     - (IBAction)openPreferences:(id)sender {
         if (!_preferencesWindowController) {
             _preferencesWindowController = [[BPPreferencesWindowController alloc] init];
         }
         [_preferencesWindowController showWindow:sender];
     }
     ```

3. **Create settings constants** in a new `BPPreferences.h`:
   ```
   extern NSString * const kBPBackgroundUpdaterIntervalKey;
   extern NSString * const kBPEnableNotificationsKey;
   extern NSString * const kBPGreedyCaskUpgradesKey;
   
   + (void)registerDefaults {
       [[NSUserDefaults standardUserDefaults] registerDefaults:@{
           kBPBackgroundUpdaterIntervalKey: @3600,  // 1 hour
           kBPEnableNotificationsKey: @YES,
           kBPGreedyCaskUpgradesKey: @NO
       }];
   }
   ```
   - Call `[BPPreferences registerDefaults]` in `BPAppDelegate.m:applicationDidFinishLaunching:` (around line 53)

4. **Access settings from anywhere:**
   ```
   NSTimeInterval interval = [[NSUserDefaults standardUserDefaults] 
       doubleForKey:kBPBackgroundUpdaterIntervalKey];
   ```

**File Locations for Settings:**
- User settings: `~/Library/Preferences/com.brunophilipe.Cakebrew.plist`
- App support: Use `BPAppDelegate.urlForApplicationSupportFolder()` (**BPAppDelegate.m:100–116**) for persistent state

---

### 5. APP SHELL — MENU BAR, NOTIFICATIONS, TIMERS, REFRESH

**Files:**
- `BPAppDelegate.h` &amp; `.m`
- `MainMenu.xib`

**Current Notification Infrastructure (BPAppDelegate.m):**

**Authorization &amp; Setup (lines 53–67):**
```
- (void)applicationDidFinishLaunching:(NSNotification *)aNotification {
    UNUserNotificationCenter *center = [UNUserNotificationCenter currentNotificationCenter];
    [center setDelegate:self];
    [center requestAuthorizationWithOptions:(UNAuthorizationOptionAlert | UNAuthorizationOptionSound)
                          completionHandler:^(BOOL granted, NSError * _Nullable error) { ... }];
}
```

**Notification Display Method (lines 158–182):**
```
- (void)requestUserAttentionWithMessageTitle:(NSString*)title 
                               andDescription:(NSString*)desc {
    // Requests app attention (dock badge)
    // Creates UNMutableNotificationContent
    // Schedules via UNUserNotificationCenter
}
```

**Delegate Methods (lines 199–213):**
- `userNotificationCenter:didReceiveNotificationResponse:withCompletionHandler:` (line 199)
- `userNotificationCenter:willPresentNotification:withCompletionHandler:` (line 207)

**Current Menu Bar Status:**
- **No `NSStatusItem` (menu bar extra) exists** — search for `statusItem` finds **zero results**
- Only dock badge support (`[[[NSApplication sharedApplication] dockTile] setBadgeLabel:]` at line 164)

**TO ADD MENU BAR ITEM &amp; BACKGROUND UPDATER:**

1. **Create Status Item** in `BPAppDelegate.m` (add to .h first):
   ```
   @interface BPAppDelegate () &lt;UNUserNotificationCenterDelegate&gt;
       @property (nonatomic, strong) NSStatusItem *statusItem;
       @property (nonatomic, strong) NSTimer *backgroundUpdaterTimer;
   @end
   
   - (void)applicationDidFinishLaunching:(NSNotification *)aNotification {
       [self setupStatusItem];  // Add this
       [self setupBackgroundUpdater];  // Add this
       // ... existing code
   }
   
   - (void)setupStatusItem {
       NSStatusBar *statusBar = [NSStatusBar systemStatusBar];
       self.statusItem = [statusBar statusItemWithLength:NSVariableStatusItemLength];
       
       NSImage *icon = [NSImage imageWithSystemSymbolName:@"terminal.fill"
           accessibilityDescription:@"Homebrew"];
       [self.statusItem.button setImage:icon];
       
       NSMenu *menu = [[NSMenu alloc] init];
       [menu addItemWithTitle:@"Outdated: 0" action:nil keyEquivalent:@""];
       [menu addItem:[NSMenuItem separatorItem]];
       [menu addItemWithTitle:@"Open Cakebrew" action:@selector(showMainWindow:) keyEquivalent:@""];
       [menu addItemWithTitle:@"Quit" action:@selector(terminate:) keyEquivalent:@""];
       
       [self.statusItem setMenu:menu];
   }
   
   - (void)setupBackgroundUpdater {
       NSTimeInterval interval = [[NSUserDefaults standardUserDefaults] 
           doubleForKey:kBPBackgroundUpdaterIntervalKey];
       self.backgroundUpdaterTimer = [NSTimer scheduledTimerWithTimeInterval:interval
           target:self selector:@selector(checkForOutdated:) userInfo:nil repeats:YES];
   }
   
   - (void)checkForOutdated:(NSTimer *)timer {
       // Trigger lightweight refresh
       [[BPHomebrewManager sharedManager] reloadFromInterfaceRebuildingCache:NO];
       // Update status item badge
       NSUInteger count = [[[BPHomebrewManager sharedManager] outdatedFormulae] count];
       if (count &gt; 0) {
           [self requestUserAttentionWithMessageTitle:@"Homebrew" 
               andDescription:[NSString stringWithFormat:@"%lu outdated", count]];
       }
   }
   ```

2. **Wire to refresh mechanism:**
   - Call `[[BPHomebrewManager sharedManager] reloadFromInterfaceRebuildingCache:NO]` 
   - Observe `homebrewManagerFinishedUpdating:` delegate callback (**BPHomebrewViewController.m:415–448**)
   - Update status item menu and send notification via `requestUserAttentionWithMessageTitle:andDescription:`

3. **Refresh Flow (Existing):**
   - `BPHomebrewManager.reloadFromInterfaceRebuildingCache:` (**BPHomebrewManager.m:57**)
   - Fetches fresh data via `BPHomebrewInterface`
   - Posts notification `homebrewManagerFinishedUpdating:` to delegate
   - In `BPHomebrewViewController.m:415–448`, updates sidebar badges, reloads table, refreshes interface

**Notification Center (UserNotifications Framework):**
- **Header import:** `#import &lt;UserNotifications/UserNotifications.h&gt;` (BPAppDelegate.m:25)
- **Delegate property:** `@interface BPAppDelegate () &lt;UNUserNotificationCenterDelegate&gt;` (line 30)
- **Display method:** `requestUserAttentionWithMessageTitle:andDescription:` (line 158)
  - Creates `UNMutableNotificationContent`
  - Schedules with `UNTimeIntervalNotificationTrigger` (0.1 sec delay)
  - Posts via `addNotificationRequest:withCompletionHandler:`

**Dock Badge:**
- Set: `[[[NSApplication sharedApplication] dockTile] setBadgeLabel:@"●"]` (line 164)
- Clear: `[[[NSApplication sharedApplication] dockTile] setBadgeLabel:nil]` (called in cleanup at line 97)

---

### 6. MENU ACTIONS &amp; ROUTING

**File:** `/Users/scottdensmore/Developer/scottdensmore/cakebrew/Cakebrew/Base.lproj/MainMenu.xib`

**Menu Structure:**
- **Main menu** (lines 16–578): Apple menu, Formula menu, Tools menu, Edit, View, Window, Help
- **Formula menu** (lines 74–239 &amp; context menu duplicate lines 592–727):
  - Actions wire to **target `547`** (BPHomebrewViewController, defined at line 743)
  - Example actions:
    - `installFormula:` (line 84)
    - `uninstallFormula:` (line 130)
    - `upgradeSelectedFormulae:` (line 148)
    - `showFormulaInfo:` (line 166)
    - **`showFormulaDependents:`** (line 178) — already exists for dependents!
    - `openSelectedFormulaWebsite:` (line 201)

**View Menu (lines 486–535):**
- Sidebar selection shortcuts (Installed, Outdated, All, Leaves, Repositories, Doctor, Update)
- **Target:** `SGP-Ky-0Th` (BPSideBarController, line 737)
- **Action:** `selectSideBarRowWithSenderTag:` (BPSideBarController.m:355–358)
- Tag values map to `FormulaeSideBarItem` enum values

**Context Menu (lines 592–727):**
- Right-click menu on formulae table
- Same actions as main Formula menu, same target

**TO ADD NEW MENU COMMANDS:**

1. **"Pin Formula" action:**
   - Add `&lt;menuItem title="Pin Formula" id="pin-item"&gt;` in Formula menu section (after "Update Formula", before separator)
   - Wire: `&lt;action selector="pinFormula:" target="547" id="..."/&gt;`
   - Implement in `BPHomebrewViewController.m`:
     ```
     - (IBAction)pinFormula:(id)sender {
         BPFormula *formula = [self selectedFormula];
         [[BPHomebrewManager sharedManager] pinFormula:formula];
     }
     ```

2. **"Manage Services" action:**
   - Add new menu item in Formula or Tools menu
   - Wire to action in `BPHomebrewViewController.m`:
     ```
     - (IBAction)showServicesManager:(id)sender {
         // Create &amp; present services window controller
     }
     ```

3. **Binding Examples (from MainMenu.xib):**
   - Conditional enable based on `currentFormula` property:
     ```
     &lt;binding destination="547" name="enabled" keyPath="self.currentFormula" id="..."&gt;
         &lt;dictionary key="options"&gt;
             &lt;string key="NSValueTransformerName"&gt;NSIsNotNil&lt;/string&gt;
         &lt;/dictionary&gt;
     &lt;/binding&gt;
     ```
   - Enable based on installation status:
     ```
     &lt;binding destination="547" name="enabled2" keyPath="self.currentFormula.installed" previousBinding="..." id="..."&gt;
         &lt;dictionary key="options"&gt;
             &lt;string key="NSValueTransformerName"&gt;NSNegateBoolean&lt;/string&gt;
         &lt;/dictionary&gt;
     &lt;/binding&gt;
     ```

**First Responder Routing:**
- Some menu actions use `target="-1"` (First Responder) to route to responder chain
- Table view can intercept via `performKeyEquivalent:` (BPFormulaeTableView.m:129–157)
- Space bar triggers `showFormulaInfo:` action (line 167–168)

**Menu Validation:**
- `BPWindow.m` implements `validateMenuItem:` to enable/disable items
- Check `self.isHomebrewInstalled` and formula selection state

---

## SUMMARY: EXACT EXTENSION POINTS

| Feature | File(s) | Lines | Action |
|---------|---------|-------|--------|
| **New Sidebar Item** | `BPSideBarController.h/.m` | 11–22, 161–210, 264–271 | Add enum value, create item, bind badge &amp; icon |
| **New List Mode** | `BPHomebrewInterface.h`, `BPHomebrewViewController.m` | 26–33, 554–608 | Add `BPListMode` enum case, wire in `sourceListSelectionDidChange` |
| **Table Columns** | `BPFormulaeTableView.m`, `BPFormulaeDataSource.m` | 11–14, 37–108, 95–143 | Define column ID, add mode case, populate data |
| **Info Pane Content** | `BPSelectedFormulaViewController.h/.m`, `BPSelectedFormula.xib` | h:25–29, m:84–168, xib:21–200+ | Add outlets, modify XIB layout, update display logic |
| **Settings** | `NSUserDefaults`, new `BPPreferences.h` | BPAppDelegate.m:53 | Create constants, register defaults, read via `standardUserDefaults` |
| **Notifications** | `BPAppDelegate.m` | 60–67, 158–182, 199–213 | Use `UNUserNotificationCenter`, call `requestUserAttentionWithMessageTitle:` |
| **Menu Bar Item** | `BPAppDelegate.m` (new code) | — | Create `NSStatusItem`, schedule `NSTimer`, update periodically |
| **Refresh Mechanism** | `BPHomebrewManager.m`, `BPHomebrewViewController.m` | 415–448 | Call `reloadFromInterfaceRebuildingCache:`, observe delegate callback |
| **Menu Actions** | `MainMenu.xib`, `BPHomebrewViewController.m` | 74–239, 617–639 | Add `&lt;menuItem&gt;` with action binding, implement `IBAction` method |

---

All coordinates are **read-only pointers** to show you exactly where to insert your code. The wiring is through Cocoa bindings, IBOutlets, and delegation patterns — no single monolithic controller, but a clean separation of concerns across sidebar, table, detail view, app delegate, and data source layers.</result>
<usage><subagent_tokens>71353</subagent_tokens><tool_uses>34</tool_uses><duration_ms>124538</duration_ms></usage>
</task-notification>

---

Lets kick off M1

---

Yes lets go on to PR2

---

testing I got a exec_bad_access when clicking the PIN menu item

---

Yes proceed

---

Yes finish M1 with the polish PR

---

Should we give a visual representation that the formula is pinned. Something next to the formula name. We can use the pin symbol part of the OS