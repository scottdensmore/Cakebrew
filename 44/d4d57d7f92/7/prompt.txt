Use the cakebrew-implement skill and spawn the cakebrew-implementer custom agent for a second review-fix pass in /tmp/cakebrew-148-notification-navigation. You own only the same eight issue files. You are not alone in the codebase: preserve all unrelated edits and do not revert work you did not create. Address only these independent-review test-bite findings under strict red-green-refactor: (1) Add an integration-level unit test proving the production notification delivery path attaches the semantic userInfo to the UNNotificationRequest content, so setting content.userInfo to nil makes the test fail. Prefer the smallest safe injectable notification-center/delivery seam; avoid global swizzling. (2) Add tests exercising BPHomebrewViewController’s real showOutdatedFormulae: and showOutdatedCasks: methods with a sidebar/outline spy, proving the exact FormulaeSideBarItem row selected so routing casks to formulae fails. (3) Refactor invalid-payload response tests so each payload independently proves exactly one cleanup, activation, window presentation, completion, and zero navigation. (4) Make every presentation spy callback accumulate thread validity with a flag initialized YES, so any off-main callback remains a failure. Observe and report the expected red before implementation. Run focused tests if possible. Do not commit, push, add XCUITests, or touch workflow configuration. Return exact red/green evidence and files changed.